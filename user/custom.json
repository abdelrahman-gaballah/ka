{
  "categories": {
    "fixed_commands": {
      "name": "System Information",
      "commands": {
        "cpu": {
          "cmd": "top -bn2 | grep 'Cpu(s)' | tail -1 | awk '{print $2 + $4}'",
          "description": "Show CPU usage percentage",
          "args": 0
        }
      }
    }
  }
}